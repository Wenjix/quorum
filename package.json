{
  "name": "quorum",
  "version": "0.2.0",
  "type": "module",
  "description": "Quorum review synthesis plus Cursor Cloud exploration DAG runner.",
  "bin": {
    "quorum": "./dist/src/cli.js",
    "quorum-cloud": "./dist/src/cli.js"
  },
  "files": [
    "dist/src/",
    "scripts/",
    "references/",
    "SKILL.md",
    "quorum.skill",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "build:skill": "git archive --format=zip --prefix=quorum/ -o quorum.skill HEAD -- SKILL.md scripts references && echo quorum.skill rebuilt from HEAD",
    "test": "npm run build && node --test dist/test/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run build && npm run build:skill && npm test"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.23"
  },
  "overrides": {
    "undici": "^6.27.0"
  },
  "overridesNote": "Security pin for @cursor/sdk's transitive undici dependency (npm audit -> 0). Do not remove until upstream no longer resolves a vulnerable undici version. Rationale: docs/dependencies.md (tracked in #5).",
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^6.0.3"
  }
}
